#include <stdio.h>
#include <stdlib.h>

int main () {
    int ch;
    while ((ch = getchar()) != EOF) {
        printf("%c", (char)ch);
    }
    
        printf("%x\n", ch);
        printf("哈哈哈哈\n");
    return 0;
}

